interface:
  display_name: "Account Digest For Reps"
  short_description: "Rank account signals into Slack digests"
  default_prompt: "Use $account-digest-for-reps to build a Slack account digest; ask who/accounts/Slack destination first, then ask which enrichment providers and prospect-update surfaces to include before signal analysis."

policy:
  allow_implicit_invocation: true
